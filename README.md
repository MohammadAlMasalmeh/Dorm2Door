# Dorm2Door

Campus services, without the schlep. React + Vite frontend; run everything from the repo root.

```bash
npm install --prefix frontend
npm run dev      # local dev
npm run build    # production build
npm run preview  # preview the build
```

That’s the gist.
